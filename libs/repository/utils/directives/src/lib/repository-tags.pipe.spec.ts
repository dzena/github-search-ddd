import { RepositoryTagsPipe } from './repository-tags.pipe';

describe('RepositoryTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new RepositoryTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
